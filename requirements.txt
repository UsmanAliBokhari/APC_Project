# SkinScan – Melanoma Detection
# Install with: pip install -r requirements.txt

opencv-python>=4.8.0        # Image I/O, CLAHE, morphology, contours, K-means
scikit-image>=0.22.0        # GLCM texture features (graycomatrix / graycoprops)
scikit-learn>=1.3.0         # RandomForestClassifier, StandardScaler, metrics
numpy>=1.24.0               # Core numerical arrays (used everywhere)
Pillow>=10.0.0              # PIL Image for Tkinter-compatible PhotoImage
joblib>=1.3.0               # Model serialisation / deserialisation
scipy>=1.11.0               # Statistical utilities (used in helpers)
